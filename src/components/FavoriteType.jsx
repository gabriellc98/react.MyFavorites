export function FavoriteTypeTitle({ type }) {
  return (
    <h3 className="text-[#CFC6B8] font-mono font-semibold text-2xl leading-tight tracking-widest uppercase mb-2">
      {type.name}
    </h3>
  );
}
